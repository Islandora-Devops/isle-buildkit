---
version: "3.8"

# Common to all services
x-common: &common
  restart: "no"

name: alpaca-servicestartswithdefaults
services:
  alpaca:
    <<: *common
    image: ${ALPACA_IMAGE:-islandora/alpaca:local}
    depends_on:
      - activemq
  activemq:
    <<: *common
    image: ${ACTIVEMQ_IMAGE:-islandora/activemq:local}
