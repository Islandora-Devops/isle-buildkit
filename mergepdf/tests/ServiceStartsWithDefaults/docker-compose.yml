---

# Common to all services
x-common: &common
  restart: "no"

name: mergepdf-servicestartswithdefaults
services:
  mergepdf:
    <<: *common
    image: ${MERGEPDF:-islandora/mergepdf:local}
    volumes:
      - ./test.sh:/test.sh # Test to run.
    command:
      - /test.sh # Run test and exit.
