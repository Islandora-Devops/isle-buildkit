mimeTypeFromDestination: true
allowedMimeTypes:
  - "audio/*"
  - "video/*"
  - "image/jpeg"
  - "image/png"
cmdByMimeType:
  default:
    cmd: "${SCYLLARIDAE_BASE_DIR}/cmd.sh"
    args:
      - "%source-mime-ext"
      - "%destination-mime-ext"
      - "%canonical"
      - "%args"
    allowInsecureArgs: ${SCYLLARIDAE_ALLOW_INSECURE_ARGS}
