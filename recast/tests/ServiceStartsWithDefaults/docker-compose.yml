---
version: "3.8"

# Common to all services
x-common: &common
  restart: "no"

name: recast-servicestartswithdefaults
services:
  recast:
    <<: *common
    image: ${RECAST:-islandora/recast:local}
    volumes:
      - ./test.sh:/test.sh # Test to run.
    command:
      - /test.sh # Run test and exit.
