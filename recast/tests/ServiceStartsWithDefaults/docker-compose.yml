---
version: "3.8"

# Common to all services
x-common: &common
  restart: "no"

services:
  recast:
    <<: *common
    image: ${RECAST_IMAGE:-islandora.dev/recast:latest}
    volumes:
      - ./test.sh:/test.sh # Test to run.
    command:
      - /test.sh # Run test and exit.
