allowedMimeTypes:
  - "*"
cmdByMimeType:
  default:
    cmd: "curl"
    args:
      - "-X"
      - "POST"
      - "-F"
      - "datafile=@-"
      - $CRAYFITS_WEBSERVICE_URI
    allowInsecureArgs: ${SCYLLARIDAE_ALLOW_INSECURE_ARGS}
